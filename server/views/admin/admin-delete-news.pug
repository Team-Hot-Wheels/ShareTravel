include admin-main.pug
 h2 Choose an article to delete 
  if (data)
   table.table.table-striped
    thead
     tr 
      th Id:
      th Title:
      th Action:
    tbody  
        each article in data
            tr
            td= article.id
            td= article.title
            td 
             form(method="post" action="/admin/admin-delete-news") 
              input(type="hidden" value=article.id name="id")
              button(type="submit" class="btn btn-default btn-danger") Delete
                