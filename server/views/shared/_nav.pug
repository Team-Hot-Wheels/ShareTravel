nav.navbar.navbar-default.navbar-fixed-top
    .container
        .navbar-header
            a.navbar-brand(href='/') Share Travel
        #navbar
            ul.nav.navbar-nav
                li.active
                    a(href='/') Home
                li
                    a(href='/trips/search') Find a trip    
                li
                    a(href='/news') News
                if(currentUser)    
                 li
                    a(href='/trips/create') Add a new trip
                if (currentUser && currentUser.username === 'admin')
                 li
                    a(href='/admin') Administration  
            form.navbar-form.navbar-left

            ul.nav.navbar-nav.navbar-right

                if (currentUser)
                    li.dropdown
                        a.dropdown-toggle(href='#', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false')= currentUser.username
                            span.caret
                        ul.dropdown-menu
                            li
                                a(href="/users/" + currentUser.username) My Profile
                            li
                                a(href="/users/update-profile") Update Profile
                            li
                                form#logout-form(method="post" action="/users/logout")
                                a(href="javascript:document.getElementById('logout-form').submit()") Logout

                else
                    li
                        a(href="/users/register") Register
                    li
                        a(href="/users/login") Log In